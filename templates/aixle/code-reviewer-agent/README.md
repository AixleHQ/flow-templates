# Code reviewer

Adds one agent, **Code reviewer**, to a project. Put it on a workflow step that
reviews a pull request or a branch: it ranks findings by severity, points each
one at a file and line with a fix, and says what it could not verify.

It brings no tools or connectors of its own. Give the step a repository and the
tools it needs to read the change.
