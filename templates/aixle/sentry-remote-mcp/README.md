# Sentry (remote MCP)

Adds Sentry's hosted MCP server (`https://mcp.sentry.dev/mcp`) to a project, so agents can read issues, stack traces and events. The server uses OAuth: after installing, sign in once from the project's MCP servers page.
